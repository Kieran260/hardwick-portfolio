import React from 'react'
import styles from '../style'

const Projects = () => {
  return (
    <section className={`flex flex-col bg-background items-center w-screen h-[200px] ${styles.paddingX}`}>
        <h1 className="text-white bg-clip-text text-2xl font-bold tracking-wide">Projects</h1>
    </section>
  )
}

export default Projects